services:
  - type: web
    name: hssiq-license-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_TOKEN
        sync: false
